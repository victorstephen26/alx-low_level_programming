#include "main.h"

/**
 * add - funtion that adds two integers and returns the result
 * @a: the first number to print
 * @b: the second number to print
 * Return: result of the addtion of both
 */

int add(int a, int b)
{
	return (a + b);
}
